package:
  id: io.github.Ultimaker.Cura
  name: 'Cura'
  version: '5.4.0'
  kind: app
  description: |
    3D printer / slicing GUI built on top of the Uranium framework

runtime:
  id: org.deepin.Runtime
  version: 23.0.0

source:
  kind: local

build:
  kind: manual
  manual:
    build: |
      make
    install: |
      make install
