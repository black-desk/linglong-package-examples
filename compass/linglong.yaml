package:
  id: COMPASS_NEWOS
  name: compass_newos
  version: 0.7.1
  kind: app
  description: |
    .

runtime:
  id: org.deepin.Runtime
  version: 20.5.0

depends:
  - id: icu
    version: 63.1
    type: runtime

source:
  kind: local

build:
  kind: manual
  manual:
    build: |
      make
    install: |
      make install