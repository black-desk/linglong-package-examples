package:
  id: io.github.stuffmatic.fSpy
  name: 'fSpy'
  version: '1.1.0'
  kind: app
  description: |
    A cross platform app for quick and easy still image camera matching

runtime:
  id: org.deepin.Runtime
  version: 23.0.0

source:
  kind: local

build:
  kind: manual
  manual:
    build: |
      make
    install: |
      make install
