package:
  id: io.github.openaudible
  name: openaudible
  version: '3.8.1'
  kind: app
  description: |
    openaudible

runtime:
  id: org.deepin.Runtime
  version: 23.0.0

source:
  kind: local

build:
  kind: manual
  manual:
    build: |
      make
    install: |
      make install
